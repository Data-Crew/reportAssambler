# pytest.ini - configuración para Pytest

[pytest]

# Suprime los DeprecationWarnings molestos que vienen de librerías externas como PyMuPDF
# (fitz usa tipos SWIG que generan warnings irrelevantes para nuestro caso)
filterwarnings =
    ignore::DeprecationWarning
